import 'package:flutter/material.dart';
import 'package:livehelp/common/widgets/icon_and_images.dart';

class IconsUtils {
  static Widget profileIcon = icons(icon: Icons.person);
  // static const Icon notificationIcon = Icon(Icons.notifications_active);
  // static const Icon helpIcon = Icon(Icons.help);
  // static const Icon mapIcon = Icon(Icons.map);
  // static const Icon bookIcon = Icon(Icons.book);
 // static const Icon callIcon = Icon(Icons.call);
}
